{
  "dependencies": {
    "ejs": "^3.1.6",
    "fs": "^0.0.1-security"
  },
  "scripts": {
    "prebuild": "node util/prebuild.js ",
    "build": " ",
    "postbuild": "node src/siteGenerator.js && open http://localhost:3000",
    "deploy": "npm run publish && npm run production:start",
    "publish": " ",
    "production:start": " "
  }
}
